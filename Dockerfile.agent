FROM python:3.10-slim

ENV PYTHONUNBUFFERED=1

WORKDIR /app

COPY pyproject.toml uv.lock ./
RUN pip install uv && uv sync --frozen --no-dev

COPY agent/ ./agent/

CMD ["sh", "-c", "uv run -m agent.agent 2>> /app/agent/logs/stderr-${BOT_ID:-agent}.log"]
