FROM python:3.10-slim

ENV PYTHONUNBUFFERED=1

WORKDIR /app

COPY pyproject.toml uv.lock ./
RUN pip install uv && uv sync --no-dev

COPY agent/ ./agent/
COPY backend/ ./backend/

CMD ["sh", "-c", "uv run uvicorn backend.main:app --host 0.0.0.0 --port 8000"]
