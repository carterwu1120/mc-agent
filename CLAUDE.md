# Minecraft Bot Agent

Active codebase is in `agent/`. See @agent/CLAUDE.md for full architecture guide.

Directories to ignore (legacy, unused):
- `fishing_tool/` — old CV-based fishing tool
- `training/` — old YOLO training scripts
